# 直接部署到 Cloudflare Pages

已停用 GitHub Actions 自動部署，改為本機直接部署。

## 前置需求

1. **Node.js** - 從 https://nodejs.org/ 安裝
2. **Cloudflare 憑證**：
   - API Token：Cloudflare Dashboard → My Profile → API Tokens → Create Token（需 Workers 編輯權限）
   - Account ID：Cloudflare Dashboard 右側欄 Workers & Pages 下方

## 部署步驟

### 方式一：使用腳本（推薦）

```powershell
# 設定環境變數（或執行時依提示輸入）
$env:CLOUDFLARE_API_TOKEN = "你的API_Token"
$env:CLOUDFLARE_ACCOUNT_ID = "你的Account_ID"

.\deploy-direct.ps1
```

### 方式二：手動執行

```powershell
npm install
npm run build
npx wrangler pages deploy dist --project-name=TIDP-SUFee
```

（需先設定 CLOUDFLARE_API_TOKEN 和 CLOUDFLARE_ACCOUNT_ID 環境變數）

## 部署後

在 Cloudflare Dashboard → Workers & Pages → TIDP-SUFee → Settings → Environment variables 設定 `GEMINI_API_KEY`（若使用 Gemini 功能）。
