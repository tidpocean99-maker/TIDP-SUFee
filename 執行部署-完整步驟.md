# 代為執行部署 - 已完成項目

## ✅ 已自動完成

1. **Git 初始化與提交**
   - 已執行 `git init`、`git add`、`git commit`
   - 主分支為 `main`
   - 已排除有問題的 `constants.tsx`

2. **建立 deploy-now.ps1**
   - 一鍵推送腳本，需提供 GitHub 使用者名稱

3. **開啟建立儲存庫頁面**
   - 已開啟 https://github.com/new?name=TIDP-SUFee

---

## 📋 請你完成（約 2 分鐘）

### 步驟 1：在 GitHub 建立儲存庫
在剛開啟的頁面中點選 **Create repository**（名稱已預填 TIDP-SUFee）

### 步驟 2：推送程式碼
在專案目錄開啟 PowerShell，執行（將 `你的帳號` 改為你的 GitHub 使用者名稱）：

```powershell
.\deploy-now.ps1 -Username 你的帳號
```

或直接執行後依提示輸入使用者名稱：

```powershell
.\deploy-now.ps1
```

### 步驟 3：設定 Secrets
推送成功後，前往：
`https://github.com/你的帳號/TIDP-SUFee/settings/secrets/actions`

新增：
- `CLOUDFLARE_API_TOKEN`
- `CLOUDFLARE_ACCOUNT_ID`

### 步驟 4：觸發部署
前往 `https://github.com/你的帳號/TIDP-SUFee/actions`，選擇 **Deploy to Cloudflare Pages** → **Run workflow**

---

## 環境限制說明

因本機未設定 `GITHUB_TOKEN` 且無 GitHub CLI，以下步驟需你手動完成：
- 建立儲存庫（需在網頁操作）
- 設定 Secrets（需在網頁操作）
- 首次 push 需你的 GitHub 帳號驗證
